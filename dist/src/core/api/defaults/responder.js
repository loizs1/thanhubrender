"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ODAutocompleteResponder_Default = exports.ODAutocompleteResponderManager_Default = exports.ODContextMenuResponder_Default = exports.ODContextMenuResponderManager_Default = exports.ODModalResponder_Default = exports.ODModalResponderManager_Default = exports.ODDropdownResponder_Default = exports.ODDropdownResponderManager_Default = exports.ODButtonResponder_Default = exports.ODButtonResponderManager_Default = exports.ODCommandResponder_Default = exports.ODCommandResponderManager_Default = exports.ODResponderManager_Default = void 0;
const responder_1 = require("../modules/responder");
/**## ODResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders`!
 */
class ODResponderManager_Default extends responder_1.ODResponderManager {
}
exports.ODResponderManager_Default = ODResponderManager_Default;
/**## ODCommandResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODCommandResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders.commands`!
 */
class ODCommandResponderManager_Default extends responder_1.ODCommandResponderManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODCommandResponderManager_Default = ODCommandResponderManager_Default;
/**## ODCommandResponder_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODCommandResponder class.
 * It doesn't add any extra features!
 *
 * This default class is made for the default `ODCommandResponder`'s!
 */
class ODCommandResponder_Default extends responder_1.ODCommandResponder {
}
exports.ODCommandResponder_Default = ODCommandResponder_Default;
/**## ODButtonResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODButtonResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders.buttons`!
 */
class ODButtonResponderManager_Default extends responder_1.ODButtonResponderManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODButtonResponderManager_Default = ODButtonResponderManager_Default;
/**## ODButtonResponder_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODButtonResponder class.
 * It doesn't add any extra features!
 *
 * This default class is made for the default `ODButtonResponder`'s!
 */
class ODButtonResponder_Default extends responder_1.ODButtonResponder {
}
exports.ODButtonResponder_Default = ODButtonResponder_Default;
/**## ODDropdownResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODDropdownResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders.dropdowns`!
 */
class ODDropdownResponderManager_Default extends responder_1.ODDropdownResponderManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODDropdownResponderManager_Default = ODDropdownResponderManager_Default;
/**## ODDropdownResponder_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODDropdownResponder class.
 * It doesn't add any extra features!
 *
 * This default class is made for the default `ODDropdownResponder`'s!
 */
class ODDropdownResponder_Default extends responder_1.ODDropdownResponder {
}
exports.ODDropdownResponder_Default = ODDropdownResponder_Default;
/**## ODModalResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODModalResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders.dropdowns`!
 */
class ODModalResponderManager_Default extends responder_1.ODModalResponderManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODModalResponderManager_Default = ODModalResponderManager_Default;
/**## ODModalResponder_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODModalResponder class.
 * It doesn't add any extra features!
 *
 * This default class is made for the default `ODModalResponder`'s!
 */
class ODModalResponder_Default extends responder_1.ODModalResponder {
}
exports.ODModalResponder_Default = ODModalResponder_Default;
/**## ODContextMenuResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODContextMenuResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders.contextMenus`!
 */
class ODContextMenuResponderManager_Default extends responder_1.ODContextMenuResponderManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODContextMenuResponderManager_Default = ODContextMenuResponderManager_Default;
/**## ODContextMenuResponder_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODContextMenuResponder class.
 * It doesn't add any extra features!
 *
 * This default class is made for the default `ODContextMenuResponder`'s!
 */
class ODContextMenuResponder_Default extends responder_1.ODContextMenuResponder {
}
exports.ODContextMenuResponder_Default = ODContextMenuResponder_Default;
/**## ODAutocompleteResponderManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODAutocompleteResponderManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.responders.autocomplete`!
 */
class ODAutocompleteResponderManager_Default extends responder_1.ODAutocompleteResponderManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODAutocompleteResponderManager_Default = ODAutocompleteResponderManager_Default;
/**## ODAutocompleteResponder_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODAutocompleteResponder class.
 * It doesn't add any extra features!
 *
 * This default class is made for the default `ODAutocompleteResponder`'s!
 */
class ODAutocompleteResponder_Default extends responder_1.ODAutocompleteResponder {
}
exports.ODAutocompleteResponder_Default = ODAutocompleteResponder_Default;
