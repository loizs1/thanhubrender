"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ODHelpMenuCategory_DefaultExtra = exports.ODHelpMenuCategory_DefaultAdvanced = exports.ODHelpMenuCategory_DefaultAdmin = exports.ODHelpMenuCategory_DefaultTicketUser = exports.ODHelpMenuCategory_DefaultTicketAdvanced = exports.ODHelpMenuCategory_DefaultTicketBasic = exports.ODHelpMenuCategory_DefaultGeneral = exports.ODHelpMenuManager_Default = void 0;
const helpmenu_1 = require("../modules/helpmenu");
/**## ODHelpMenuManager_Default `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the global variable `opendiscord.helpmenu`!
 */
class ODHelpMenuManager_Default extends helpmenu_1.ODHelpMenuManager {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuManager_Default = ODHelpMenuManager_Default;
/**## ODHelpMenuCategory_DefaultGeneral `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:general` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultGeneral extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultGeneral = ODHelpMenuCategory_DefaultGeneral;
/**## ODHelpMenuCategory_DefaultTicketBasic `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:ticket` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultTicketBasic extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultTicketBasic = ODHelpMenuCategory_DefaultTicketBasic;
/**## ODHelpMenuCategory_DefaultTicketAdvanced `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:ticket` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultTicketAdvanced extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultTicketAdvanced = ODHelpMenuCategory_DefaultTicketAdvanced;
/**## ODHelpMenuCategory_DefaultTicketUser `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:ticket` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultTicketUser extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultTicketUser = ODHelpMenuCategory_DefaultTicketUser;
/**## ODHelpMenuCategory_DefaultAdmin `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:admin` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultAdmin extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultAdmin = ODHelpMenuCategory_DefaultAdmin;
/**## ODHelpMenuCategory_DefaultAdvanced `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:advanced` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultAdvanced extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultAdvanced = ODHelpMenuCategory_DefaultAdvanced;
/**## ODHelpMenuCategory_DefaultExtra `default_class`
 * This is a special class that adds type definitions & typescript to the ODHelpMenuManager class.
 * It doesn't add any extra features!
 *
 * This default class is made for the `opendiscord:general` category in `opendiscord.helpmenu`!
 */
class ODHelpMenuCategory_DefaultExtra extends helpmenu_1.ODHelpMenuCategory {
    get(id) {
        return super.get(id);
    }
    remove(id) {
        return super.remove(id);
    }
    exists(id) {
        return super.exists(id);
    }
}
exports.ODHelpMenuCategory_DefaultExtra = ODHelpMenuCategory_DefaultExtra;
